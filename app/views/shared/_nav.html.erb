
<div class='logo'>
  <h1>Time Tracker</h1>
</div>

<% if current_user %>
<ul class="custom-nav nav nav-tabs">
  <li class="<%= active?(root_path) %>">
    <%= link_to "", root_path, class: 'glyphicon glyphicon-home' %>
  </li>

  <%= render 'shared/employee_nav_items' if employee? %>

  <%= render 'shared/audit_log_tab' if admin? %>


  <li class="dropdown pull-right">
     <a href="#" data-toggle="dropdown" class="dropdown-toggle">Account<strong class="caret"></strong></a>
    <ul class="dropdown-menu">

      <%= render 'shared/admin_dropdown' if admin? %>

      <li>
        <%= link_to "Logout", destroy_user_session_path, method: :delete if user_signed_in? %>
        <%= link_to "Login", new_user_session_path unless user_signed_in? %>
      </li>
    </ul>
  </li>
</ul>
<% end %>

<!-- # those active calls are for showing the page you're currently on -->